To start runing the app, run the app "app.js" with node.
Then run the command "npm start" on the folder Searcher.
A browser window should appear with the react app with a botton
that reads "Login to Spotify", by login into Spotify, the access token
will be retreived and the app should be runing.